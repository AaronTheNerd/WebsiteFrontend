import { ProjectDetailDirective } from "./project-detail.directive";

describe("ProjectDetailDirective", () => {
  it("should create an instance", () => {
    const directive = new ProjectDetailDirective();
    expect(directive).toBeTruthy();
  });
});
